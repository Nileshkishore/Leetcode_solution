class Solution:
    def reversePrefix(self, word: str, ch: str) -> str:
        if ch not in word:
            return word
        else:
            i=word.index(ch)
            print(i)
            print(word[i::-1])
            return word[i::-1]+word[i+1:]
