class Solution:
    def countSegments(self, s: str) -> int:
        s=s.strip()
        if s=="":
            return 0
        j=s.split(" ")
        count=0
        for i in j:
            if i!='':
                count+=1
        return count
