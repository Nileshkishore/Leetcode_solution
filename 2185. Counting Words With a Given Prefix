class Solution:
    def prefixCount(self, words: List[str], pref: str) -> int:
        l=len(pref)
        count=0
        for i in words:
            if i[:l]==pref :
                count+=1
        return count
