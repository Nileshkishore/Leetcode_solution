class Solution:
    def findSpecialInteger(self, arr: List[int]) -> int:
        res=[]
        for i in arr :
            if arr.count(i)/ len(arr) > 0.25 :
                res.append(i)
        print(res)
        return res[0]
