class Solution:
    def splitWordsBySeparator(self, words: List[str], separator: str) -> List[str]:
        r=[]
        for i in words:
            j=i.split(separator)
            for k in j:
                if k!='':
                    r.append(k)
        return r
