class Solution:
    def findChampion(self, grid: List[List[int]]) -> int:
            res=[]
            for i in grid:
                res.append(i.count(1)) 
            print(res)
            return(res.index(max(res)))
